import{mkdir,readFile,writeFile}from'node:fs/promises';import ts from'typescript';
for(let route of ['shared','projects/route','project/route','session/route','comments/route','members/route']){let source=await readFile('app/api/'+route+'.ts','utf8');source=source.replace('"cloudflare:workers"','"../env.mjs"').replace("'cloudflare:workers'","'../env.mjs'").replace("from'../shared'","from'../shared.mjs'");let output=ts.transpileModule(source,{compilerOptions:{target:ts.ScriptTarget.ES2022,module:ts.ModuleKind.ES2022}}).outputText;await mkdir('standalone/api/'+route.split('/').slice(0,-1).join('/'),{recursive:true});await writeFile('standalone/api/'+route+'.mjs','// Generated from app/api/'+route+'.ts. Regenerate with pnpm prepare:standalone.\n'+output)}
