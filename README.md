# Astrum Studio

Astrum Studio is an independent plain HTML/JavaScript 3D editing, animation and WebGPU rendering workspace.

The recovered source, standalone packages, tests, documentation and GitHub Pages build are being imported into this repository. The Pages edition uses browser-local project storage; the included server edition retains its database and collaboration APIs.

This initial release is not complete Cinema 4D parity or a production-qualified replacement.
