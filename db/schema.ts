import { sqliteTable, text, integer, index } from 'drizzle-orm/sqlite-core';
export const projects=sqliteTable('projects',{id:text('id').primaryKey(),owner:text('owner').notNull(),name:text('name').notNull(),scene:text('scene').notNull(),revision:integer('revision').notNull().default(1),updatedAt:integer('updated_at').notNull()},t=>[index('projects_owner').on(t.owner)]);
export const members=sqliteTable('members',{id:text('id').primaryKey(),projectId:text('project_id').notNull(),email:text('email').notNull(),role:text('role').notNull()},t=>[index('members_project').on(t.projectId),index('members_email').on(t.email)]);
export const comments=sqliteTable('comments',{id:text('id').primaryKey(),projectId:text('project_id').notNull(),author:text('author').notNull(),text:text('text').notNull(),objectId:text('object_id'),frame:integer('frame'),createdAt:integer('created_at').notNull()},t=>[index('comments_project_created').on(t.projectId,t.createdAt)]);
export const presence=sqliteTable('presence',{id:text('id').primaryKey(),projectId:text('project_id').notNull(),name:text('name').notNull(),selection:text('selection').notNull(),updatedAt:integer('updated_at').notNull()},t=>[index('presence_project').on(t.projectId)]);
